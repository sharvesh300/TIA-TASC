import { NextResponse } from "next/server";

// Implemented in Step 6 — triggers extraction for a PipelineJob.
export async function POST() {
  return NextResponse.json({ error: "Not implemented yet" }, { status: 501 });
}
