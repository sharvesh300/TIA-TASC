import { NextResponse } from "next/server";

// CRUD for clients, users, rules — built alongside the (admin) pages.
export async function GET() {
  return NextResponse.json({ error: "Not implemented yet" }, { status: 501 });
}
