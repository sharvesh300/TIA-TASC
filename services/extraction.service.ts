import type { ExtractionResult, CanonicalRow } from "@/types/extraction";

// Implemented in Step 6 — normalises agent output into ExtractedRow-ready canonical rows.
export function normalizeExtraction(_result: ExtractionResult): CanonicalRow[] {
  throw new Error("normalizeExtraction not implemented yet");
}
